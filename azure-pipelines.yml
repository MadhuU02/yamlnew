resources:
  repositories:
  - repository: yamlnew
    type: github
    endpoint: github.com_MadhuU02
    name: MadhuU02/yamlnew
  - repository: armTemplate # In a different organization
    endpoint: armConnection
    type: git
    name: other/armTemplate

trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

steps:
- checkout: self
- checkout: yamlrepo
- checkout: armTemplate

- script: dir $(Build.SourcesDirectory)